language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-testable-module' ]; then cd .. && eval "mv $currentfolder generator-testable-module" && cd generator-testable-module; fi

